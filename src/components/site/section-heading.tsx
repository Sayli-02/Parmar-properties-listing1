import type { ReactNode } from "react";export function SectionHeading({eyebrow,title,copy,action}:{eyebrow:string;title:string;copy?:string;action?:ReactNode}){return <div className="mb-10 flex flex-col gap-6 md:mb-14 md:flex-row md:items-end md:justify-between"><div><p className="eyebrow mb-4">{eyebrow}</p><h2 className="section-title text-balance max-w-3xl">{title}</h2>{copy&&<p className="mt-5 max-w-2xl leading-7 text-muted-foreground">{copy}</p>}</div>{action}</div>}
