import { useState } from "react";import { useNavigate } from "@tanstack/react-router";import { MapPin, Search } from "lucide-react";import { Button } from "@/components/ui/button";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
export function SearchBar({hero=false}:{hero?:boolean}){const navigate=useNavigate();const [location,setLocation]=useState("all");const [bhk,setBhk]=useState("all");const go=()=>navigate({to:"/properties",search:{location,bhk}} as never);const Field=({label,value,set,items}:{label:string;value:string;set:(v:string)=>void;items:string[]})=><div className="min-w-0 flex-1"><span className="mb-1 block text-[10px] font-bold uppercase tracking-[.14em] text-muted-foreground">{label}</span><Select value={value} onValueChange={set}><SelectTrigger className="h-8 border-0 bg-transparent px-0 shadow-none"><SelectValue/></SelectTrigger><SelectContent>{items.map(x=><SelectItem key={x} value={x.toLowerCase().replaceAll(" ","-")}>{x}</SelectItem>)}</SelectContent></Select></div>;
 return <div className={`grid gap-2 border border-border bg-card p-3 shadow-2xl shadow-background/30 md:grid-cols-[1.2fr_1fr_1fr_1fr_auto] md:items-center md:gap-0 md:divide-x md:divide-border ${hero?"":""}`}><Field label="Location" value={location} set={setLocation} items={["All","Worli","Bandra","Juhu","South Mumbai","Powai"]}/><Field label="Configuration" value={bhk} set={setBhk} items={["All","2 BHK","3 BHK","4 BHK","5 BHK"]}/><Field label="Budget" value="all" set={()=>{}} items={["All","Under ₹5 Cr","₹5–10 Cr","₹10 Cr+"]}/><Field label="Property type" value="all" set={()=>{}} items={["All","Apartment","Penthouse","Duplex"]}/><Button size="lg" onClick={go} aria-label="Search properties"><Search/> <span className="md:hidden xl:inline">Search</span></Button></div>}
