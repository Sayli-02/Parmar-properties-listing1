import { createFileRoute,Link } from "@tanstack/react-router";import { Heart,Scale,Trash2 } from "lucide-react";import { Button } from "@/components/ui/button";import { PropertyCard } from "@/components/site/property-card";import { properties } from "@/lib/properties";import { useSiteState } from "@/lib/site-state";
export const Route=createFileRoute("/saved")({head:()=>({meta:[{title:"Saved Properties — Parmar Properties"},{name:"description",content:"Review and compare your saved Mumbai properties."},{property:"og:title",content:"Saved Properties — Parmar Properties"},{property:"og:description",content:"Review and compare your selected Mumbai homes."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Saved});function Saved(){const {saved,compared,toggleCompared}=useSiteState();const items=properties.filter(p=>saved.includes(p.slug));return <main className="page-shell min-h-[70vh] pb-24 pt-40"><p className="eyebrow mb-4">Your shortlist</p><div className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><h1 className="section-title">My Saved Properties</h1>{compared.length>1&&<Button asChild><Link to="/compare"><Scale/>Compare selected ({compared.length})</Link></Button>}</div>{items.length?<div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(p=><div key={p.slug}><PropertyCard property={p}/><label className="mt-3 flex items-center gap-3 text-sm text-muted-foreground"><input type="checkbox" checked={compared.includes(p.slug)} onChange={()=>toggleCompared(p.slug)} className="size-4 accent-[var(--color-gold)]"/> Select for comparison</label></div>)}</div>:<div className="mt-16 border border-border py-20 text-center"><Heart className="mx-auto size-8 text-gold"/><h2 className="mt-5 font-display text-3xl">Your collection is waiting.</h2><p className="mx-auto mt-3 max-w-md text-muted-foreground">Save the homes that speak to you and return to them here.</p><Button asChild className="mt-7"><Link to="/properties">Explore properties</Link></Button></div>}</main>}
